<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Declared so the permissions e2e scenario has something real to
         grant and revoke. The app never uses them; it only reports what
         it has been granted. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <application
        android:allowBackup="false"
        android:label="Tales Demo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TalesDemo">

        <!-- Handling these in-process keeps a configuration change from
             recreating the activity mid-scenario. A CI emulator changes
             them on its own (the soft keyboard resizing the window, a
             density or uiMode refresh after a package update), and a
             recreation there rewinds the app under test to its first
             screen, which surfaces as a Tales failure naming an element
             that was on screen a moment earlier. -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation|uiMode|density|locale|layoutDirection|fontScale"
            android:exported="true"
            android:label="Tales Demo"
            android:theme="@style/Theme.TalesDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
