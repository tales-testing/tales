version: "2"

run:
  timeout: 3m
  tests: true
linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - misspell
issues:
  exclude-use-default: false
